# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node / Vite
node_modules/
dist/
.vite/
*.log

# Env / secrets
.env
.env.local
.env.*.local

# Runtime / logs
logs/
*.pid
# Note: package lock files (uv.lock, package-lock.json) are COMMITTED for reproducibility.

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Build artifacts
build/
out/
*.tar
*.tar.gz

# OMC / Claude Code session artifacts (never commit)
.omc/
.claude/
# Note: CLAUDE.md 는 레포 문서로 사용하므로 commit 함 (session artifact 아님)
